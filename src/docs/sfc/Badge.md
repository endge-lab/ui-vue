# Badge

`Badge` рендерит компактную метку и сохраняет `tone` как renderer metadata.

```vue
<Badge :tone="flight.statusTone">{{ flight.status }}</Badge>
```

Поддержанные props: `tone`, `tooltip`, spacing/style props.
