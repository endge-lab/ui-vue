# Flex

`Flex` рендерит flex-контейнер.

```vue
<Flex row gap="2" align="center">
  <Text>{{ flight.number }}</Text>
  <Badge>{{ flight.status }}</Badge>
</Flex>
```

Поддержанные props: `row`, `col`, `direction`, `gap`, `align`, `justify`, `wrap`, spacing/style props.
