# Number

`Number` рендерит числовое значение через `Intl.NumberFormat`.

```vue
<Number :value="flight.delay" suffix=" min" />
```

Поддержанные props: `value`, `prefix`, `suffix`, `decimals`, `minFractionDigits`, `maxFractionDigits`.
